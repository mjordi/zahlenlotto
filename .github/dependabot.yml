version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "09:00"
      timezone: "Europe/Berlin"
    open-pull-requests-limit: 10
    commit-message:
      prefix: "chore"
      include: "scope"
    ignore:
      # Temporary compatibility ceiling: the current Next.js/ESLint stack does not support TypeScript 7.
      # Remove after the TypeScript 7 upgrade passes CI and Vercel.
      - dependency-name: "typescript"
        versions:
          - ">=7.0.0"
    groups:
      react:
        patterns:
          - "react"
          - "react-dom"
          - "@types/react"
          - "@types/react-dom"
      next:
        patterns:
          - "next"
          - "eslint-config-next"
      testing:
        patterns:
          - "@testing-library/*"
          - "jest"
          - "jest-environment-jsdom"
          - "@types/jest"
      tailwind:
        patterns:
          - "tailwindcss"
          - "@tailwindcss/*"
